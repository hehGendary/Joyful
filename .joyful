s = input(">>>")

size = 30'000
nums = array(size)
el = 0
for (i = 0, i < strlen(s), i = i + 1) {
    current = char(s, i)
    if (current == ">") {
        el = (el + 1) % size
    }
    if (current == "<") {
         el = (el + size - 1) % size
    }
    if (current == "+") {
        size[el] = size[el] + 1
    }
    if (current == "-") {
        size[el] = size[el] - 1
    }
    if (current == ".") {
        print(numToChar(size[el]))
    }
    if (current == ",") {

    }

}